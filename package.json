{
  "name": "@readest/monorepo",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "eslint": "^8",
    "husky": "^9.1.6",
    "prettier": "^3.3.3",
    "typescript": "^5"
  }
}
